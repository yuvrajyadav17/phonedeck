# PhoneDeck dependencies.
#
#   python -m pip install -r requirements.txt
#
# Everything here has wheels for Python 3.14. Notably `winsdk` does NOT -- it
# fails to build -- which is why the media API uses the split `winrt-*`
# packages instead.

# --- server ---------------------------------------------------------------
flask>=3.1
psutil>=7.0

# --- desktop editor window and tray ---------------------------------------
pywebview>=6.0
pystray>=0.19
pillow>=11.0

# --- "now playing", via the Windows media session API ---------------------
winrt-runtime>=3.0
winrt-Windows.Media.Control>=3.0
winrt-Windows.Foundation>=3.0
winrt-Windows.Foundation.Collections>=3.0
winrt-Windows.Storage.Streams>=3.0

# --- streaming PC audio to the phone --------------------------------------
# soundcard captures the default output with WASAPI loopback; flask-sock
# carries the PCM over a WebSocket.
soundcard>=0.4
numpy>=2.0
flask-sock>=0.7

# --- voice input: offline speech recognition ------------------------------
# Needs a model too, which is not a pip package:
#   https://alphacephei.com/vosk/models/vosk-model-small-en-us-0.15.zip
#   unzip into .tools/  (about 40 MB download, 68 MB on disk)
vosk>=0.3.45

# --- tools/devtools.py: evaluate JS inside the phone's WebView ------------
websocket-client>=1.8
